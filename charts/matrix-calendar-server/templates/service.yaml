apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrix-calendar-server.fullname" . }}
  labels:
    {{- include "matrix-calendar-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matrix-calendar-server.selectorLabels" . | nindent 4 }}
